import React from 'react';

// import burgerLogo from '../../assets/images/';
import './Logo.scss';

const Logo = (props) => (
  <div className="Logo" style={{ height: props.height }}>
    {/* <img src={burgerLogo} alt="sitelogo" /> */}
  </div>
);

export default Logo;
